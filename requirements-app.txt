# Core dependencies for the bundled app
fastapi>=0.115
uvicorn[standard]>=0.34
jinja2>=3.1
python-multipart>=0.0.18
aiofiles>=24.1
google-api-python-client>=2.160
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
anthropic>=0.42
apscheduler>=3.10
httpx>=0.28
aiosqlite>=0.20
python-dotenv>=1.0
cryptography>=42

# Social media
tweepy>=4.14
Mastodon.py>=1.8

# Transcription. mlx-whisper runs on Apple Silicon GPU (Metal via MLX) and is
# 30–50× faster than faster-whisper, which on Apple Silicon is forced onto the
# CPU because CTranslate2 has no Metal backend. Install both: mlx-whisper as
# the fast default for Apple Silicon, faster-whisper as the universal
# fallback for Intel Macs.
mlx-whisper>=0.4,<1; platform_machine=="arm64" and sys_platform=="darwin"
faster-whisper>=1.0

# YouTube video download — needed when the user re-transcribes an imported
# (no-local-file) video locally with Apple Speech / Whisper. pytubefix needs
# no cookies; for private videos the route flips them to unlisted on
# user confirmation before downloading.
pytubefix>=10,<11
